<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.tylerbwong.stack">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".presentation.StackApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.DeepLinkingActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Accepts URIs that begin with "https://www.stackoverflow.com/questions” -->
                <data
                    android:scheme="https"
                    android:host="stackoverflow.com"
                    android:pathPrefix="/questions" />

                <data
                    android:scheme="https"
                    android:host="stackoverflow.com"
                    android:pathPrefix="/q" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.questions.detail.QuestionDetailActivity"
            android:theme="@style/QuestionDetailTheme"/>
    </application>

</manifest>
